# Simple Todo App
This is a basic todo app hosted on https://dotnet-todos.netlify.app/. Built with React and TypeScript, it connects to a .NET backend hosted on Railway that uses an in-memory database.

